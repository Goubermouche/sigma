#pragma once

namespace sigma {
	class function_registry {
		// local functions (symbol_table_key)
		// -   handle<ast::function>
		// -   handle<ir::function>

		// external functions (symbol_table_key)
		// -   function
		// -   external_function
	};
} // namespace sigma
